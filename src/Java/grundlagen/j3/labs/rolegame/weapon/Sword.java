package Java.grundlagen.j3.labs.rolegame.weapon;

public class Sword extends MeleeWeapon {
    public Sword(String weaponName, int weight, double attackValue, int defenseValue) {
        super(weaponName, weight, attackValue, defenseValue);
    }
}
