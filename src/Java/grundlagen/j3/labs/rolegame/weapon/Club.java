package Java.grundlagen.j3.labs.rolegame.weapon;

public class Club extends MeleeWeapon {
    public Club(String weaponName, int weight, double attackValue, int defenseValue) {
        super(weaponName, weight, attackValue, defenseValue);
    }
}
